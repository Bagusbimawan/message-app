'use client';

// Placeholder AuthProvider — no-op for UI preview mode.
// Replace with real Firebase onAuthStateChanged when backend is ready.
export default function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
